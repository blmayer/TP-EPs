/*
 * This is the first exercise: implement functions to manipulate linked lists.
 */

#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

void main(void) {

    /* Create an empty list */
    dllist *ages;
    puts("1");
    initialize(ages);
    puts("2");
    if(ages -> prev == NULL) {}
    puts("null");
    printf("%d\n", ages -> val);
}

